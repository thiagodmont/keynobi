// Re-export bridge — all new code should import from "@/components/ui"
// This bridge will be removed in the App Integration plan (Phase 3)
export { VirtualList, type VirtualListProps, type VirtualListHandle } from "@/components/ui/VirtualList";
