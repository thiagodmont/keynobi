// Re-export bridge — remove after Phase 5 (App Integration)
export { DialogHost, showDialog } from "@/components/ui/Dialog";
export { showSaveDialog, showCloseDialog } from "@/lib/dialogs";
export type { SaveDialogResult, CloseDialogResult } from "@/lib/dialogs";
export type { DialogButton, DialogButtonStyle } from "@/components/ui/Dialog";
